export const environment = {
  production: false,
  // URL de l'API locale par défaut (Spring Boot: 8080, Node.js: 3000, etc.)
  apiUrl: 'http://localhost:8080/api', 
  // Passer à true si on veut utiliser les données mockées pour le moment
  useMocks: true 
};
